{
  "config": {
    "step": {
      "user": {
        "title": "Push Image",
        "description": "Create an image entity updated via webhook. The webhook receives JSON containing an image URL.",
        "data": {
          "name": "Name",
          "json_key": "JSON key containing image URL",
          "device_name_key": "Optional JSON key containing device name",
          "device_name_filter": "Optional device name filter value",
          "ssl_verify": "Verify SSL certificate when fetching image URL",
          "token": "Optional token required in X-Push-Image-Token header"
        }
      }
    },
    "error": {
      "name_required": "Name is required.",
      "json_key_required": "JSON key is required.",
      "device_name_key_required": "Device name key is required when using a device name filter."
    }
  }
}
